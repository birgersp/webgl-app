#ifdef GL_ES
precision mediump float;
#endif

uniform bool useColor;

varying vec4 vColor;

void main() {
    if (useColor == true) {
      gl_FragColor = vColor;
    } else {
        gl_FragColor = vec4(0,0,0,1);
    }
}
