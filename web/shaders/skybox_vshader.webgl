uniform mat4 view;
uniform mat4 projection;

attribute vec3 position;

varying vec3 textureCoord;

void main() {
  mat4 fixedView = mat4(view[0], view[1], view[2], vec4(0, 0, 0, 1));
  gl_Position = projection * fixedView * vec4(position, 1);
  textureCoord = position;
}
